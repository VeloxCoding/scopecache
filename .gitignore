/scopecache
*.exe
*.test
/sock/
/.claude/scheduled_tasks.lock
/CLAUDE.md
/CLAUDE_TODO.md
/CLAUDE_COMMENTS.md
/CLAUDE_ADDON_DRAINER.md
/CLAUDE_PHPEXTENSION_IN_GO.md
/CLAUDE_OPTIMALISATIE.md
/_phase4/
/init.txt
/caddy-module-plan.md
/cross-cutting-concerns.md
/guardedflow.md
/core-and-addons.md
/archive/
/bench-results/
/deploy/Caddyfile.caddyscope.bench-backup
/Caddyfile.caddyscope.bench-backup
# Roundtrip benchmark docs: pre-v0.8.19 numbers, need full rewrite
# after the /get, /head, /tail, /scopelist, write-envelope perf work.
# Kept locally as reference while the rewrite is pending.
/docs/benchmark_roundtrip.md
/docs/benchmark_roundtrip_setup.md
# Scripts are local-only operator/dev tooling; only the explicitly
# tracked exceptions below ship with the repo:
#   - install_caddyscope.sh: one-shot fresh-VPS installer (Ubuntu/
#     Debian) that downloads the caddyscope binary from a GitHub
#     release, sets up the systemd unit, and verifies /help.
#   - run_benchmark.sh: client-side helper to seed a running scopecache
#     instance with a small dataset and report throughput.
/scripts/*
!/scripts/install_caddyscope.sh
!/scripts/run_benchmark.sh
# Integrated demo/test scaffolding — local-only, not part of the
# shipped repo: the go-sqlite Caddy module (a wrapper around
# ncruces/go-sqlite3) and the Docker harness that runs Caddy +
# FrankenPHP + scopecache + go-sqlite together.
/tools/go-sqlite-module/
/tools/frankenphp-docker/

# go.work + go.work.sum bind root + caddymodule for local dev (so a single
# `go test ./...` run sees both modules without round-tripping through
# proxy.golang.org). Per Go's own docs this file is per-machine and
# should not be committed; consumers (`go install`, `xcaddy --with …`)
# resolve through go.mod / caddymodule/go.mod alone.
/go.work
/go.work.sum
